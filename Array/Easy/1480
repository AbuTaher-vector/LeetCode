class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        
         vector<int>a;
     long long int x=0;
     for(int i=0;i<nums.size();i++)
     {
        x+=nums[i];
        a.push_back(x);

     }
        return a;
        
    }
};
