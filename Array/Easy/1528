class Solution {
public:
    string restoreString(string s, vector<int>& indices) {
        int x=s.size();
        vector<char>a(x);
        for(int i=0;i<indices.size();i++) a[indices[i]]=s[i];
        string ss;
        for(auto x:a) ss.push_back(x);
        return ss;
        
    }
};
