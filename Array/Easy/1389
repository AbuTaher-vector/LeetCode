class Solution {
public:
    vector<int> createTargetArray(vector<int>& x, vector<int>& y) {
       vector<int>ans;
        for(int i=0;i<y.size();i++)
        {
            ans.insert(ans.begin()+y[i],x[i]);
        }
        return ans;
        
    }
};
