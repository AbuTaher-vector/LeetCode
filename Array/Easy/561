class Solution {
public:
    int arrayPairSum(vector<int>v) {
         long long sum=0;
    sort(v.begin(),v.end());
    for(int i=0;i<v.size();i=i+2) sum+=min(v[i],v[i+1]);
        
    return sum;
        
    }
};
