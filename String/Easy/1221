class Solution {
public:
    int balancedStringSplit(string s) {
        int a=0,b=0,ans=0;
        for(auto x:s)
        {
            if(x=='L') a++;
            else b++;
            
            if(a==b) ans++;
        }
        return ans;
        
    }
};
