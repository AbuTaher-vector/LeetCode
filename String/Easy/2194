class Solution {
public:
    vector<string> cellsInRange(string s) {
        vector<string>v;
        for(char i=s[0];i<=s[3];i++)
        {
            for(char j=s[1];j<=s[4];j++)
            {
                string x;
                x.push_back(i);
                x.push_back(j);
                v.push_back(x);
            }
        }
        return v;
        
    }
};
